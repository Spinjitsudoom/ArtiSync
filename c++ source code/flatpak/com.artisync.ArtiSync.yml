app-id: com.artisync.ArtiSync
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: ArtiSync

finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --share=network
  - --filesystem=xdg-music:rw
  - --filesystem=xdg-download:rw
  - --filesystem=xdg-documents:rw
  - --filesystem=/run/media:rw
  - --filesystem=/mnt:rw
  - --talk-name=org.freedesktop.Notifications

modules:
  # nlohmann/json is header-only; not in KDE SDK so we build+install it first.
  - name: nlohmann-json
    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DJSON_BuildTests=OFF
      - -DJSON_Install=ON
    sources:
      - type: git
        url: https://github.com/nlohmann/json.git
        tag: v3.11.3

  # Main application. curl and taglib come from the KDE SDK (6.10).
  - name: ArtiSync
    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
    post-install:
      - install -Dm644 resources/ArtiSync.png
          /app/share/icons/hicolor/256x256/apps/com.artisync.ArtiSync.png
      - install -Dm644 flatpak/com.artisync.ArtiSync.desktop
          /app/share/applications/com.artisync.ArtiSync.desktop
      - install -Dm644 flatpak/com.artisync.ArtiSync.metainfo.xml
          /app/share/metainfo/com.artisync.ArtiSync.metainfo.xml
    sources:
      - type: dir
        path: ..
